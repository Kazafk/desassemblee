name: Mise à jour des scores politiques

on:
  schedule:
    - cron: "0 6 * * 1"   # Chaque lundi à 6h UTC
  workflow_dispatch:        # Déclenchement manuel possible

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Installer les dépendances
        run: pip install -r scripts/requirements.txt

      - name: Mettre en cache les données API CLAIR (sessions archivées)
        uses: actions/cache@v4
        with:
          path: data/raw/clair/
          key: clair-cache-${{ github.run_id }}
          restore-keys: |
            clair-cache-

      - name: Télécharger les nouveaux votes (session active)
        run: python scripts/fetch_clair.py

      - name: Calculer les scores gauche-droite
        run: python scripts/compute_scores.py

      - name: Construire scores.json
        run: python scripts/build_data.py

      - name: Commit automatique si scores.json modifié
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "data: mise à jour scores politiques [${{ github.event.schedule || 'manuel' }}]"
          file_pattern: web/data/scores.json data/processed/scores_computed.json
          commit_user_name: "github-actions[bot]"
          commit_user_email: "github-actions[bot]@users.noreply.github.com"
